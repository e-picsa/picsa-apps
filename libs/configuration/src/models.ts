import type { IConfiguration } from './types';

export const cloneConfiguration = (id: string): IConfiguration => {
  // TODO - deep clone an existing configuration to create a new one
  return null as any;
};

class ConfigurationClass {
  constructor() {
    //
  }
}
