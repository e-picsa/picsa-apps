import * as schema from './schema_V0';

// Re-export schema to provide latest version without need to refactor additional code

export const FEEDBACK_QUEUE_COLLECTION = schema.FEEDBACK_QUEUE_COLLECTION;
export type IFeedbackQueueEntry = schema.IFeedbackQueueEntry;
export const FEEDBACK_QUEUE_SCHEMA = schema.SCHEMA_V0;
