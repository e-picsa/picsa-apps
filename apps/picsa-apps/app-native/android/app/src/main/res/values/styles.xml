<?xml version="1.0" encoding="utf-8"?>
<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>


  <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>

  <!--  Theme used during splash -->
  <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
    <!-- Android 12+ splash screen attributes -->
    <item name="windowSplashScreenBackground">@color/white</item>
<!--    NOTE animated, needs padding... currently use default app icon -->
<!--    <item name="windowSplashScreenAnimatedIcon">@drawable/picsa_splash_animated</item>-->

    <!-- Pre-Android 12 fallback -->
    <item name="android:windowBackground">@drawable/picsa_splash_static</item>
  </style>
</resources>
