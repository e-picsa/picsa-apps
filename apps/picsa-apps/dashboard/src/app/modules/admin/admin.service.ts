import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class DashboardAdminService {
  public listAuthUsers() {
    // TODO - add admin endpoint that validates user jwt role and handles
  }
}
