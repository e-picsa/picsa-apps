# Optional: required only by the Kobo sync function.
KOBO_API_KEY=

# Required by budget/migrate-firebase and the shared Firebase Firestore client.
FIREBASE_API_KEY=
FIREBASE_PROJECT_ID=

# Optional: Firestore database name. Defaults to (default) when omitted.
FIREBASE_DATABASE_ID=(default)
