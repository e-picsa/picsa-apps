{
  "$schema": "..\\..\\..\\node_modules\\nx\\schemas\\project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/picsa-server/docker",
  "prefix": "picsa",
  "implicitDependencies": ["picsa-server-scripts"],
  "targets": {
    "start": {
      "executor": "./tools/executors/docker:docker",
      "dependsOn": [{ "projects": "dependencies", "target": "docker-prepare" }],
      "outputs": [],
      "options": {
        "commands": ["compose --project-name picsa-server up"],
        "cwd": "apps/picsa-server/docker"
      }
    }
  },
  "tags": []
}
