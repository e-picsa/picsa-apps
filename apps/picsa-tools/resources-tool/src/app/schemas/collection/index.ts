import * as schema from './schema_v0';

// Re-export schema to provide latest version without need to refactor additonal code

export const COLLECTION_COLLECTION = schema.COLLECTION_V0;
export type IResourceCollection = schema.IResourceCollection_v0;
export const COLLECTION_SCHEMA = schema.SCHEMA_V0;
