import * as schema from './schema_v0';

// Re-export schema to provide latest version without need to refactor additonal code

export const LINKS_COLLECTION = schema.COLLECTION_V0;
export type IResourceLink = schema.IResourceLink;
export const LINKS_SCHEMA = schema.SCHEMA_V0;
