import type { Database } from '@picsa/server-types';

/**
 * Server database row type for monitoring forms
 */
export type IMonitoringFormsRow = Database['public']['Tables']['monitoring_forms']['Row'];


